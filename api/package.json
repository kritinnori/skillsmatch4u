{
  "name": "api",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "setup-db": "bun run src/setup-db.ts",
    "seed": "bun run src/seed-auto.ts"
  },
  "dependencies": {
    "mongodb": "^6.9.0",
    "elysia": "^1.1.0",
    "@elysiajs/cors": "^1.1.0",
    "openai": "^4.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
